[project]
name = "gwen-train"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.11",
    "transformers>=5.0",
    "datasets",
    "accelerate",
    "huggingface-hub",
    "tqdm",
]

[dependency-groups]
dev = [
    "ipython",
]
